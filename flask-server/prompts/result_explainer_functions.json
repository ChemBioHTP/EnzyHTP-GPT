[
  {
    "name": "calculate_mean",
    "description": "Compute the arithmetic mean of a list of numeric values.",
    "strict": false,
    "parameters": {
      "type": "object",
      "properties": {
        "values": {
          "type": "array",
          "items": {
            "type": "number"
          },
          "description": "Numeric values to average."
        }
      },
      "required": [
        "values"
      ]
    }
  },
  {
    "name": "calculate_standard_deviation",
    "description": "Compute the standard deviation of a list of numeric values. Use ddof=1 for sample SD or ddof=0 for population SD.",
    "strict": false,
    "parameters": {
      "type": "object",
      "properties": {
        "values": {
          "type": "array",
          "items": {
            "type": "number"
          },
          "description": "Numeric values to summarize."
        },
        "ddof": {
          "type": "integer",
          "description": "Delta degrees of freedom. Use 1 for sample SD (default) or 0 for population SD."
        }
      },
      "required": [
        "values"
      ]
    }
  }
]
